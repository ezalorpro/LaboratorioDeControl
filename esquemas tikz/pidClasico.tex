\documentclass[tikz, border=0mm, convert={density=300,size=1080x800,outext=.png}]{standalone}

\usetikzlibrary{shapes,arrows,positioning}

\begin{document}

\tikzset{%
    block/.style={draw, fill=white, rectangle, 
            minimum height=2em, minimum width=3em},
    input/.style={inner sep=0pt},       
    output/.style={inner sep=0pt, node distance=0.6cm},      
    sum/.style = {draw, fill=white, circle, minimum size=2mm, inner sep=0pt},
    pinstyle/.style = {pin edge={to-,thin,black}},
    branch/.style={fill,circle,minimum size=3pt,outer sep=-1pt,inner sep=0pt, node distance=0.6cm}
}

\begin{tikzpicture}[auto, >=latex']

    \node[input] (input) {};
    \node [sum, right = of input] (sum) {};
    \node [block, right = of sum] (controller) {$PID$};
    \node [block, right = of controller] (system) {$G(s)$};
    \node [branch, right = of system] (yjoint) {};
    \node [output, right = of yjoint] (output) {};

    \draw [draw,->] (input) node[above right] {$r(t)$} -- (sum);
    \draw [->] (sum) -- node {$e(t)$} (controller);
    \draw [->] (controller) -- node [name=sct] {$sc(t)$} (system);
    \draw [-] (system) -- (yjoint);
    \draw [->] (yjoint) -- (output) node [name=y, above left] {$y(t)$};

    \node [block, below = of sct] (sensor){$H(s)$};

    \draw [->] (yjoint.south) |- (sensor);

    \draw [->] (sensor) -| node[pos=0.99, right] {$-$} 
        node [pos=.25, above] {} (sum);

\end{tikzpicture}
\end{document}